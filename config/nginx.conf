worker_processes 1;

events { worker_connections 1024; }

http {
    server {
        listen  8080;
        root    /app/dist;
        index   index.html;

        location / {
            try_files $uri /index.html;
        }
    }
}
